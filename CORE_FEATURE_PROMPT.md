# INMO Card Lab 0.1 核心功能提示词

## 可复用提示词

```text
你是一名 Android 与计算机视觉工程师。请开发并持续优化一个运行在 INMO Air2 智能眼镜上的 21 点实时出牌记录原型，应用名为 INMO Card Lab。

目标设备与约束：
- 设备：INMO Air2 智能眼镜。
- 系统：Android 9（API 28），最低兼容 API 26。
- CPU/ABI：32 位 armeabi-v7a，约 2 GB RAM。
- 显示：物理分辨率 640x400，系统可能缩放为 1280x800。
- 相机：固定使用 Camera ID 0，Camera2 LIMITED，分析分辨率 640x480，目标帧率 5-15 FPS。
- 禁止依赖会在该 CPU 上触发 XNNPACK SIGILL 的 ML Kit/TensorFlow Lite 原生 OCR；0.1 使用纯 Kotlin 模板识别。
- 所有识别、统计和数据存储必须离线完成，不上传相机画面或牌局数据。

核心场景：
- 游戏为 21 点（Blackjack），使用两副标准扑克牌，共 104 张。
- 每个牌面 A、2-10、J、Q、K 初始各有 8 张。
- 相机实时识别桌面上进入中央识别框的牌角数字或字母。
- 同一牌面连续识别 3 次才确认记录；确认后必须连续 2 次检测不到牌，才允许记录下一张，避免重复计数。
- 每次确认后写入本地 SQLite，包括会话、牌面和时间；App 重启后恢复当前牌靴。
- 支持撤销最后一张牌；长按重开可建立新的两副牌会话。

概率规则：
- 每张牌出现后，从对应牌面的剩余数量减 1，并以“该牌面剩余数 / 全部剩余牌数”计算下一张概率。
- 13 个牌面分别显示剩余数量与下一张出现概率，并按概率从高到低排序。
- 顶部显示四类 21 点概率：低牌 2-6、中性牌 7-9、十点牌 10/J/Q/K、A。
- 初始概率应为：低牌 40/104，中性牌 24/104，十点牌 32/104，A 8/104。
- 所有概率必须根据已记录牌实时更新；不得把统计概率描述为确定预测。

眼镜界面：
- 使用黑色、高对比、低干扰的全屏界面，适配眼镜横屏。
- 相机预览以较低透明度作为背景，中央显示黄色识别框。
- 顶部显示识别状态、最近记录牌、剩余张数和已出张数。
- 中部显示四类概率及 13 个单牌面概率。
- 底部提供撤销、长按重开、省电显示、暂停相机四个控制键。
- 省电显示隐藏相机预览但保留低频图像分析；暂停相机则完全解绑相机。
- 状态文字只有变化时才刷新，避免无意义重绘和系统服务唤醒。

工程要求：
- Kotlin Android 应用，使用 CameraX 1.3.4 与 Camera2 interop。
- 通过 CameraX 配置仅暴露 Camera ID 0，绕过设备错误的相机列表验证。
- 使用单线程分析器、KEEP_ONLY_LATEST 背压策略和 RGBA_8888 输入。
- 识别器裁剪中央 ROI，执行亮度检查、Otsu 二值化、连通域提取、字形归一化及模板相似度匹配。
- 概率引擎、去重状态机和数据库职责分离；概率规则必须有单元测试。
- Manifest 同时声明普通 LAUNCHER 与 LEANBACK_LAUNCHER，确保 INMO 启动器可发现应用。

0.1 验收标准：
- APK 可安装并从 INMO Air2 启动器或 ADB 正常打开。
- Camera 0 能持续预览和分析，不出现 SIGILL 或 native crash。
- 初始显示 104 张，四类概率总和为 100%。
- 记录、撤销、重开及重启恢复均能正确更新本地数据与概率。
- 省电显示和暂停相机行为符合定义。

后续迭代方向：
- 使用真实牌桌样本校准不同字体、角度、光照和运动模糊下的识别阈值。
- 区分玩家手牌、庄家明牌和其他已出牌后，再增加玩家补牌爆牌率、庄家结果分布等功能。
- 测量相机、屏幕与 CPU 功耗，动态降低帧率、ROI 尺寸和界面刷新频率。
```

## 0.1 范围说明

本版本是离线原型，核心目标是稳定记录已出现牌面并显示剩余牌的数学概率。它尚未区分牌桌座位、玩家手牌或庄家明牌，因此不计算具体手牌的补牌爆牌率，也不提供确定性结果预测。
